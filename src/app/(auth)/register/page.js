import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Create Account | Salon Booking Platform',
  description: 'Create your account to start booking appointments or register your salon.',
};

export default function RegisterPage() {
  return <SignupForm />;
}
